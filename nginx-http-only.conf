server {
    listen 80;
    listen [::]:80;
    server_name verify.lecocon.app;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://verify.lecocon.app$request_uri;
    }
}
